{
  "buildCommand": "npm install --legacy-peer-deps && prisma generate && next build",
  "installCommand": "npm install --legacy-peer-deps && prisma generate",
  "env": {
    "PRISMA_GENERATE_SKIP_AUTOINSTALL": "true"
  }
}
